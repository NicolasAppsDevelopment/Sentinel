{% extends 'base.html.twig' %}

{% block body %}
<table class="devices-table">
        <thead>
            <tr>
                <th>MAC Address</th>
                <th>IP Address</th>
            </tr>
        </thead>
        <tbody>
            {% for device in devices %}
                <tr>
                    <td>{{ device.mac_address }}</td>
                    <td>{{ device.ip }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
