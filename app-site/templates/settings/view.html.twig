{% extends 'base.html.twig' %}

{% block body %}
    {{ component('navigation', {index: 2}) }}
    <div class="content">
        <form>
            <h2>Sync server time</h2>
            <div class="form-control" id="serverTimeContainer">
                <label>You device time</label>
                <input type="datetime-local" id="clientTime" readonly />
            </div>
            <div class="form-control" id="serverTimeContainer">
                <label for="serverTime">Sentinel's time</label>
                <input type="datetime-local" name="server-time" id="serverTime" value="{{ serverTime }}" required autofocus />
            </div>
            <button class="btn-primary w100" type="submit">
                <i class="fa-solid fa-sync"></i> Update Sentinel's time
            </button>
        </form>

        <h2>Your Detections</h2>
        <a href="{{ path('user_detections_delete') }}" class="card action">
            <span class="fa-solid fa-trash red"></span>
             <p class="desc">Remove all </p>
        </a>

        {{ form_start(accessPointForm) }}
            <h2>Access Point Configuration</h2>

            {% for error in errors %}
                <div class="form-message error">
                    <img src="{{ asset('icons/info.svg') }}" alt="Error">
                    <p>{{ error }}</p>
                </div>
            {% endfor %}

            <div class="form-control">
                <label for="accessPointName">Name</label>
                <input type="text" name="{{ field_name(accessPointForm.accessPointName) }}" value="{{ field_value(accessPointForm.accessPointName) }}" id="accessPointName" autocomplete="accessPointName">
            </div>
            <div class="form-control">
                <label for="accessPointPassword">Password</label>
                <input type="password" name="{{ field_name(accessPointForm.accessPointPassword) }}" value="{{ field_value(accessPointForm.accessPointPassword) }}" id="accessPointPassword" autocomplete="accessPointPassword">
            </div>
            <div class="form-control">
                <label for="accessPointPasswordVerify">Password Confirmation</label>
                <input type="password" name="{{ field_name(accessPointForm.accessPointPasswordVerify) }}" value="{{ field_value(accessPointForm.accessPointPasswordVerify) }}" id="accessPointPassword" autocomplete="accessPointPassword">
            </div>

            <button class="btn-primary w100" type="submit">
                <i class="fa-solid fa-sync"></i> Update access point Configuration
            </button>
        {{ form_end(accessPointForm) }}

        {{ form_start(deactivationRangeForm) }}
            <h2>Deactivation range</h2>

            {% for error in errors %}
                <div class="form-message error">
                    <img src="{{ asset('icons/info.svg') }}" alt="Error">
                    <p>{{ error }}</p>
                </div>
            {% endfor %}

            <h3>Monday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="mondayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.mondayFrom) }}" value="{{ field_value(deactivationRangeForm.mondayFrom) }}" id="mondayFrom">
                </div>
                <div class="form-control w100">
                    <label for="mondayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.mondayTo) }}" value="{{ field_value(deactivationRangeForm.mondayTo) }}" id="mondayTo">
                </div>
            </div>

            <h3>Tuesday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="tuesdayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.tuesdayFrom) }}" value="{{ field_value(deactivationRangeForm.tuesdayFrom) }}" id="tuesdayFrom">
                </div>
                <div class="form-control w100">
                    <label for="tuesdayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.tuesdayTo) }}" value="{{ field_value(deactivationRangeForm.tuesdayTo) }}" id="tuesdayTo">
                </div>
            </div>

            <h3>Wednesday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="wednesdayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.wednesdayFrom) }}" value="{{ field_value(deactivationRangeForm.wednesdayFrom) }}" id="wednesdayFrom">
                </div>
                <div class="form-control w100">
                    <label for="wednesdayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.wednesdayTo) }}" value="{{ field_value(deactivationRangeForm.wednesdayTo) }}" id="wednesdayTo">
                </div>
            </div>

            <h3>Thursday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="thursdayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.thursdayFrom) }}" value="{{ field_value(deactivationRangeForm.thursdayFrom) }}" id="thursdayFrom">
                </div>
                <div class="form-control w100">
                    <label for="thursdayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.thursdayTo) }}" value="{{ field_value(deactivationRangeForm.thursdayTo) }}" id="thursdayTo">
                </div>
            </div>

            <h3>Friday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="fridayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.fridayFrom) }}" value="{{ field_value(deactivationRangeForm.fridayFrom) }}" id="fridayFrom">
                </div>
                <div class="form-control w100">
                    <label for="fridayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.fridayTo) }}" value="{{ field_value(deactivationRangeForm.fridayTo) }}" id="fridayTo">
                </div>
            </div>

            <h3>Saturday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="saturdayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.saturdayFrom) }}" value="{{ field_value(deactivationRangeForm.saturdayFrom) }}" id="saturdayFrom">
                </div>
                <div class="form-control w100">
                    <label for="saturdayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.saturdayTo) }}" value="{{ field_value(deactivationRangeForm.saturdayTo) }}" id="saturdayTo">
                </div>
            </div>

            <h3>Sunday</h3>
            <div class="row w100">
                <div class="form-control w100">
                    <label for="sundayFrom">From</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.sundayFrom) }}" value="{{ field_value(deactivationRangeForm.sundayFrom) }}" id="sundayFrom">
                </div>
                <div class="form-control w100">
                    <label for="sundayTo">To</label>
                    <input type="time" name="{{ field_name(deactivationRangeForm.sundayTo) }}" value="{{ field_value(deactivationRangeForm.sundayTo) }}" id="sundayTo">
                </div>
            </div>

            <button class="btn-primary w100" type="submit">
                <i class="fa-solid fa-sync"></i> Update activation time
            </button>
        {{ form_end(deactivationRangeForm) }}

        <script>
            // Met à jour l'affichage de l'heure locale du navigateur
            function updateClientTime() {
                document.getElementById('clientTime').value = getNowStandardDate();
            }

            function getNowStandardDate() {
                const datetime = new Date();
                let d = datetime.getDate();
                let m = datetime.getMonth() + 1; //Month from 0 to 11
                let y = datetime.getFullYear();
                let h = datetime.getHours();
                let min = datetime.getMinutes();

                const formattedDate = `${y}-${m<=9 ? '0' + m : m}-${d <= 9 ? '0' + d : d}`;
                const formattedTime = `${h<=9 ? '0' + h : h}:${min<=9 ? '0' + min : min}`;

                return formattedDate + " " + formattedTime;
            }

            window.addEventListener('load', () => {
                setInterval(updateClientTime, 10000);
            });
        </script>
    </div>
{% endblock %}
