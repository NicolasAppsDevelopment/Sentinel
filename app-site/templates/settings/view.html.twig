{% extends 'base.html.twig' %}

{% block body %}
    {{ component('navigation', {index: 2}) }}
    <div class="content">
        <div class="form-control" id="serverTimeContainer">
            <label>Date et heure du client</label>
            <input
                    type="datetime-local"
                    id="clientTime"
                    value=""
                    readonly
            />
        </div>
        <form 
            hx-post="{{ path('app_settings_sync_time') }}"
            hx-target="#serverTimeContainer"
            hx-swap="outerHTML"
        >
            <div class="form-control" id="serverTimeContainer">
                <label for="serverTime">Date et heure du serveur</label>
                <input
                    type="datetime-local"
                    name="server-time"
                    id="serverTime"
                    value="{{ serverTime }}"
                    required
                    autofocus
                />
            </div>

            <button class="btn-primary w100" type="submit">
                <i class="fa-solid fa-sync"></i> Mettre à jour la date et heure du serveur
            </button>
        </form>

        {# <button class="btn-primary w100" type="button"
            hx-get="{{ path('app_settings_sync_time') }}"
            hx-target="#serverTime"
            hx-swap="outerHTML">
            <i class="fa-solid fa-sync"></i> Mettre à jour la date et heure du serveur
        </button> #}

        <script>
            // Met à jour l'affichage de l'heure locale du navigateur
            function updateClientTime() {
                document.getElementById('clientTime').value = getNowStandardDate();
            }

            function getNowStandardDate() {
                const datetime = new Date();
                let d = datetime.getDate();
                let m = datetime.getMonth() + 1; //Month from 0 to 11
                let y = datetime.getFullYear();
                let h = datetime.getHours();
                let min = datetime.getMinutes();

                const formattedDate = `${y}-${m<=9 ? '0' + m : m}-${d <= 9 ? '0' + d : d}`;
                const formattedTime = `${h<=9 ? '0' + h : h}:${min<=9 ? '0' + min : min}`;

                return formattedDate + " " + formattedTime;
            }


            // On exécute au chargement, puis on met à jour toutes les secondes
            window.addEventListener('load', () => {
                updateClientTime();
            });
        </script>
    </div>
{% endblock %}
